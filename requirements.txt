anthropic>=0.40
google-genai>=0.3
pyyaml>=6.0
pydantic>=2.6
requests>=2.31
tenacity>=8.2
pytest>=8.0
# The linter, declared so a lint receipt is reproducible rather than a property of whichever
# machine happens to have it. Rule set is pinned in ruff.toml; without it ruff's own defaults
# drift between versions and two checkouts disagree about whether the tree is clean.
ruff>=0.16
python-json-logger>=2.0
# Porter stemming for fixture key matching (retrieval.py::_stem). The import IS guarded
# (try/except -> no stemming), so a clean install does not crash — it silently matches
# fixture keys differently. That is worse than a crash and is exactly the divergence
# tests/test_suite_is_machine_independent.py exists to catch: "incumbency" stems to
# "incumb" on a machine that happens to have nltk and does not on one that does not.
# PorterStemmer is pure algorithm, so this pulls in no corpus download.
nltk>=3.9
# Markdown -> HTML for the bundle's self-contained index.html reading experience
# (bridge.py::_create_bundle -> pack_html.py). Imported at module top level (not lazy) since
# pack_html.py is loaded on every publish, not just an optional path.
mistune>=3.3
# The typeset edition of the pack (pack_pdf.py -> Complete_Pack.pdf). Imported LAZILY inside
# the renderer, unlike mistune above: an install without it must still publish, one bonus file
# short, rather than break the bundle writer on the money rail. The vendored OFL fonts it
# embeds live in prospector/assets/fonts/ — fpdf2's core fonts are latin-1 only, which would
# silently drop the £ sign and every em-dash in engine prose.
fpdf2>=2.8
# pack_pdf.py imports fontTools DIRECTLY (to read the vendored OFL metrics), and today it
# resolves only because fpdf2 happens to pull it in transitively — the same accident the
# numpy note below describes. A direct import must be declared, or the day fpdf2 drops the
# edge the typeset pack breaks on a clean install and nothing here predicted it.
fonttools>=4.5
# Test-only: reads back the PDF this repo just wrote, so the assertions are about what a
# buyer's reader will see (page count, extracted text) rather than about our own byte layout.
pypdf>=5.0
# API layer (api.py) + its FastAPI test client
fastapi>=0.110
uvicorn>=0.29
starlette>=0.36
httpx>=0.27
# Cloudflare R2 (S3-compatible) deliverable upload — bridge.py R2Uploader. Without it the R2 path
# silently disables and packs publish unlisted (list-only-after-upload).
boto3>=1.34
# Operator control center (supervised dashboard) + its tests
streamlit>=1.40
# Three dependencies the engine imports LAZILY, inside the function that needs them:
#   stripe   -> bridge.py:911         (money rail: product/price provisioning)
#   ddgs     -> retrieval.py:656      (DuckDuckGo search provider)
#   exa_py   -> retrieval.py:461      (Exa search provider)
# All three were installed on the dev machine and never declared here. A lazy import means
# nothing fails at boot — the process starts clean and dies at the first call that reaches
# the provider, which for `stripe` is the first attempt to price a pack. Found on 2026-07-31
# by running the suite in a venv built ONLY from this file; CI never reported them because
# it runs pytest with -x and had been stopping on an earlier failure.
stripe>=15.2
ddgs>=9.14
exa_py>=2.14
# F3 (COMMERCIAL_READINESS_PROGRAM.md): the buyer's editable XLSX financial model, written
# with live `=`-formulas so the model can be re-run rather than just read. Pure Python, so
# unlike torch/onnxruntime/matplotlib it HAS a wheel for this venv's python 3.14 x86_64 —
# that constraint is why F2's radar chart is hand-written SVG instead of matplotlib.
openpyxl>=3.1
# tools/meta_shape_monitor.py imports numpy at module top level, and
# tests/unit/test_meta_shape_monitor.py is in the CI suite. It resolves today only because
# streamlit happens to pull numpy in transitively, which is precisely the accident this file
# exists to stop: the machine-independence gate
# (tests/test_suite_is_machine_independent.py:179) parametrizes over prospector/**/*.py ONLY,
# so a direct import under tools/ is invisible to it and would break the day streamlit drops
# the transitive edge.
numpy>=1.26
# Added 2026-08-15 for the pack's own quality floor, after the founder read a rebuilt pack and
# asked why nothing had caught its repetition. All three are imported INSIDE the function that
# uses them, on the same terms as stripe/ddgs/exa_py above — and unlike those three they are
# declared here from the day they are first imported, which is the whole point of that note.
#
# rapidfuzz    -> pack_linter.check_repetition. The near-duplicate pass is O(n^2) over ~500
#                 sentences; difflib.SequenceMatcher took over two minutes on one pack, which
#                 is a gate nobody would leave switched on. C++ core, a wheel, no build step.
# trafilatura  -> retrieval.fetch_page_text, as a FALLBACK below 200 characters, which is a
#                 narrower claim than the one first written here and the narrower claim is the
#                 measured one. A/B over the same bytes on 12 cited URLs from pack
#                 e698149e137fc164: boilerplate was a TIE (4 furniture hits each), so "it
#                 removes nav bars" was wrong. What it actually fixes is total extraction
#                 failure — the lxml landmark ladder returned 15 chars for isbe.net and 96 for
#                 geekwire, and usable passages went 9/12 -> 12/12. It runs only when the cheap
#                 path already failed because it costs ~2s/page against lxml's ~0.05s.
# textstat     -> pack_linter.readability_grades, RECORDED and never actuated. Measured on the
#                 same pack, the fourteen sections span grade 5.9-17.3, and the worst is "Copy
#                 you can paste" at 17.3 — the section whose job is to hand the buyer sentences
#                 for a landing page. That number is the finding. It is not a threshold, because
#                 the spread tracks who WROTE each section, and a grade ceiling would block a
#                 pack for quoting a statute.
rapidfuzz>=3.14
trafilatura>=2.2
textstat>=0.7
# NOTE: the LUX packages (lux-popdd / lux-spec / lux-spec-cli) are NOT listed here.
# They were pinned to absolute file:/// paths on one developer's machine, which made this
# file uninstallable anywhere else — including CI and any deploy host. Nothing in
# prospector/ or tests/ imports them; only run_v2.py and scripts/popdd_verify.py do.
# They now live in requirements-local.txt, which is optional. See that file to install them.
