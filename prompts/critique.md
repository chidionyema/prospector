USER: Critique every one of these opportunities. Each carries an `idx` — echo it back
unchanged so your critique can be matched to its idea.

{candidates_json}

Return exactly {n} critique objects, in input order. Do not drop any.
