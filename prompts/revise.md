USER: Rewrite every one of these opportunities to act on its critique. Each carries an
`idx` — echo it back unchanged so your revision can be matched to its original.

{candidates_json}

Return exactly {n} revised objects, in input order. Do not drop any.
