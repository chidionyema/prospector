/**
 * /rejected is the public name (founder brief 2026-09-02 §8).
 * /kill-log 301s here. The page body is still the ledger; copy is renamed in follow-up commits.
 */
export { default, getServerSideProps } from "./kill-log";
