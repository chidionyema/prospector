"""One declaration file per project, and the schema that refuses a bad one.

Clause A7 of `docs/GOLD_STANDARD_SPEC.md`: adding a second project is a declaration, never a
code change. `schema.py` is what makes that claim checkable.
"""
