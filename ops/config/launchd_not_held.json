{
  "_what": "Labels declared in ops/launchd/ that launchd is deliberately NOT holding.",
  "_how": "scripts/launchd_plists.py --assert-held fails on every declared label launchd is not holding UNLESS the label appears here with a reason. The reason is required and must be checkable by someone who was not there: cite the doc, the decision or the switch.",
  "_why_json_not_yaml": "This file is read by a script the docs run under the system python3, which has no PyYAML. The launchd declarations beside it are JSON for the same reason.",
  "not_held": {
    "ai.hermes.cockpit": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:cockpit] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.coordinator": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:coordinator] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.gateway": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:gateway] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again. Exactly one Telegram long-poller may exist; HERMES_GATEWAY_AUTOSTART in fly.toml decides which machine owns it.",
    "ai.hermes.ngrok": "the ngrok tunnel is off on purpose -- scripts/process_audit.py says so in the grade_launchd comment, and nothing on this estate depends on a public tunnel today",
    "ai.hermes.otto-server": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:otto-server] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.progress": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:progress] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.rsi": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:rsi] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.submodule-backup": "ported to Fly and disabled here on 2026-08-19 22:47 to end a real split -- the same daemon ran on prospector-hermes AND on this Mac against separate SQLite databases, so every answer depended on which one you asked. ~/.hermes/deploy/hermes/supervisord.conf [program:submodule-backup] runs it now, ~/.hermes/config/primary_environment declares `fly`, and ~/.hermes/scripts/check_single_environment.sh fails when it is loaded here again.",
    "ai.hermes.watchdog": "it is the outer ring that kickstarts ai.hermes.gateway and ai.hermes.coordinator when their pids vanish. Both left this Mac for Fly on 2026-08-19, so here it would try to restart two disabled jobs and DM the founder every five minutes for ever. ~/.hermes/deploy/hermes/supervisord.conf:11 names it as one of the four that do not move, because supervisord is the watchdog over there.",
    "com.prospector.consumer": "runs on Fly on the same machine as the scheduler; it publishes, so it touches money -- docs/ENGINE_MIGRATION_PROGRAM.md:444",
    "com.prospector.live-update": "it exists to roll the prospector-live laptop checkout forward; on Fly a deploy is the update, so it dies at cutover -- docs/ENGINE_MIGRATION_PROGRAM.md:448. The operator's switch store/scheduler/NO_AUTO_UPDATE (created 2026-08-18) says the same thing.",
    "com.prospector.scheduler": "the engine runs on Fly (prospector-engine) since the 2026-08-18 cutover -- docs/ENGINE_MIGRATION_PROGRAM.md:443",
    "com.prospector.watchdog": "restarts a stalled tick, which is useless on a machine that is off; Fly's own restart policy owns this now -- docs/ENGINE_MIGRATION_PROGRAM.md:445 and EDGE-7 at :147"
  },
  "_no_excuse_yet": "ai.hermes.selfcheck is deliberately NOT excused. It is the hourly invariant checker, written on 2026-08-19 after 25 liveness scripts missed two defects that had been live for weeks. It was disabled in the same sweep, and it is in neither list in ~/.hermes/deploy/hermes/supervisord.conf, so it now runs nowhere at all. Leaving it red is the point: this file excuses jobs that are off by design, not jobs that were lost. Excuse it only once it runs somewhere, and change the reason to say where."
}
