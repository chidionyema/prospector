# What is true in EVERY environment. An overlay may change an image tag, a hostname, a replica count
# or which Secret is referenced. An overlay may never change a policy: staging with softer rules
# proves nothing about production.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - engine.yaml
  - api.yaml
  - web.yaml

# STILL NOT HERE, and not pretended otherwise. The engine, the store API and the storefront moved
# on 2026-08-24 (register row F-39); the edge proxy and the CI runner are still generated inline by
# deploy/targets/k8s.sh and are still refused by this estate's own policies. Each manifest here
# carries its own header saying what about it is declared and what is measured — api.yaml's
# `runAsUser: 10001`, for one, has not been drilled the way engine.yaml's has.
