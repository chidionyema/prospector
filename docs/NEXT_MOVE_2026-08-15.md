# Next move — one item: the shelf's false clean sheet is on **all 59** live packs, not 73 of 75

**Date:** 2026-08-15 · **Status:** OPEN, blocker reproduced below · **Owner:** engine

One item, the evidence it is real, the exact edits, and the commands that decide it is done.
This supersedes `docs/NEXT_MOVE_2026-08-14.md`: same defect, **measured against R2 for the first
time**, and the numbers in that ticket were read off the wrong artifact.

---

## The item

**Re-render `QA_Report.md` from the stored record for every pack on the shelf and re-upload it, so
no pack a buyer downloads states it cleared checks its own report shows it failed.**

Not the `.md`-removal work (in flight, uncommitted in this checkout — `prospector/bridge.py`
`PACK_DOCUMENTS`/`BUNDLE_FILES` split, `bridge.py:284`/`:310`). Not the 5 stranded PASSes. Not
storefront v5. This is a false factual claim inside the artefact **every** live listing sells, and
it contradicts the single proposition the product rests on — that the checks are real and
published.

## The evidence — measured against R2 today, not disk

The 2026-08-14 ticket measured `publish/bundles/` and reported 73 of 75. That is the local build
directory, which a backfill leaves stale by design (uploads are content-addressed to a new key,
`prospector/bridge.py:1215-1224`); the same mistake is already recorded as a correction in
`docs/HANDOFF_PACK_CONTENTS_REVIEW.md`. Reading what the store actually serves — catalogue
`GET /catalog` → `GET /internal/catalog/{id}/content` → the R2 object, the path
`tools/preview_packs.py:87-121` uses — the defect is **total, not partial**:

```
catalogue packs        : 59
  fetch errors         : 0
  no QA_Report.md      : 0
  RETIRED banner       : 59
  ...and contradicted  : 11
offenders: ['904b6a0bd834ae08', '2abc23c3c0d05bab', '1723d378cff66ebc', '08b22037fc2afc07',
 '13d41ccee9e96e2d', 'cee21a292219c61a', '871ba1e244fba2d4', '8d5e24fbe6c1f5d3',
 'd4ef1efc5328d8cf', '86a2b4eb9a66df28', '176ec4f0fb04c4a5']
```

Run twice, the second with a cold cache so every zip was genuinely re-fetched. Quoted from the
**live** object for `08b22037fc2afc07` (296,894 bytes, 14 entries), member `QA_Report.md`:

```
line    9: _This cleared every check we hold it to, on evidence we fetched and cited below._
line  104: ### ❌ Can the customer afford it?
line  106: **No — the sources contradict this.** Confidence 0.53. *(check: `payer_solvency`)*
```

The generator is already correct — `prospector/dossier.py` `_pass_gloss` counts verdicts instead
of asserting them (fixed 2026-08-14). The fix reaches packs built *after* it and never reaches the
shelf, because `tools/backfill_bundle_html.py:20-28` copies every `.md` deliverable
byte-identical by design.

**Why the disk number understated it:** disk said 73/75 stale / 12 contradicted; R2 says 59/59
stale / 11 contradicted. Disk holds packs that were never listed and misses re-uploads. Only the
R2 read is the product.

## The blocker, reproduced today

The re-render needs a stored dossier. Every stored PASS dossier still raises:

```
stored PASS dossiers=64 rendered_ok=0 raised=64
FIRST FAILURE: store/dossiers/08b22037fc2afc07.pass.json
        for ax, val in sc.scores.items():
                       ^^^^^^^^^^^^^^^
    AttributeError: 'types.SimpleNamespace' object has no attribute 'items'
```

(62 on 2026-08-14, 64 today — the count grows with every PASS, so this gets worse, not stale.)

Cause, not symptom: `pack_manifest._ns` (`prospector/pack_manifest.py:350-354`) descends into
*every* dict, including `score.scores` and `score.justification`, whose keys are data (axis names),
not fields. `dossier.py` `_verdict_of` already carries the "one reader, both shapes" lesson for
verdicts; the score maps never got it.

## The work order

1. **Make a stored dossier renderable.** Add a `_mapping()` reader at `prospector/dossier.py:671`
   the way `_verdict_of` reads verdicts — reader side, where the shape ambiguity is already
   handled — rather than teaching `_ns` which dicts are data. Acceptance is `raised=0` on the
   snippet above.
2. **Extend the backfill to regenerate `QA_Report.md`** when the stored record contradicts the
   pack's banner. Second deliberate exception to the byte-identical rule; document it in
   `tools/backfill_bundle_html.py`'s docstring next to the first (`:20-28`), same narrowness.
   Content-compared, so re-running is a no-op. Packs with no stored dossier fall back to the
   zip's `manifest.jsonld` (`tools/backfill_bundle_html.py:31-35`).
3. **Give the probe an R2 mode — this is what makes "done" provable.**
   `scripts/pack_banner_probe.py` reads `publish/bundles/` only; its own docstring says disk is
   the floor and R2 is the proof, and its `main()` never fetches. As written it can report PASS
   while all 59 live packs are stale, which is exactly how yesterday's ticket read as nearly-done.
   Give it `--from {disk,r2}` reusing `tools/preview_packs.py`'s `fetch_catalogue` / `_s3` /
   `_content_key` / `zip_for`, default `r2`.
4. **Commit the probe.** `git ls-files --error-unmatch scripts/pack_banner_probe.py` → *"did not
   match any file(s) known to git"*. The only guard against this returning is untracked. Commit it
   and wire it into the same gate as `scripts/site_spec_probe.py`.
5. **Re-upload, then re-verify from R2**, not from disk.

⚠ Another session is live in this checkout (197 uncommitted paths, `prospector/bridge.py` among
them). Do not `git add -A`; add the specific files.

## Acceptance — the commands that decide this is done

```bash
# 1. every stored PASS dossier renders, no exception
.venv/bin/python -m pytest -q tests/unit/test_pack_manifest.py tests/unit/test_backfill_bundle_html.py

# 2. the probe can see the product at all
set -a; . .env; set +a
.venv/bin/python scripts/pack_banner_probe.py --from r2     # must exit 0

# 3. no regression elsewhere
.venv/bin/python -m pytest -q
```

Done means **`pack_banner_probe.py --from r2` exits 0**, on a cold cache. Not that the code
changed, and not that the disk probe went green.

## Explicitly not this ticket

- **5 PASS packs stranded off the shelf** (`.venv/bin/python tools/verify_pass_shelf_coverage.py`
  → `stranded passes: 5`; 4 never published, 1 lint-blocked with 6 errors). Real revenue loss and
  the runner-up item — but it is 5 packs against 59, and 4 of the 5 need a diagnosis before a fix
  exists, so it is not yet a ship item.
- **Thin `Marketing_Assets`** — 8 of 59 live packs under 150 words against a shelf that promises
  "listing page, outreach, social" (`docs/HANDOFF_PACK_CONTENTS_REVIEW.md`). Generation quality,
  needs its own owner.
- **The `.md`-out-of-the-zip change** — already in flight, uncommitted in this checkout.
- **Generation reliability** — the 2026-08-15T00:21Z `tick_hard_deadline` critical alert is
  resolved by `9f03ced`; ticks at 03:55/04:56/06:01/06:06Z carry `error: None` and the daemon is
  writing dossiers (`store/dossiers/3ef77fb293e745c7.kill.json`, 06:08Z). `ALERT.txt` is stale.

## How the R2 census above was produced

`tools/preview_packs.py` already holds every primitive; the census is 30 lines around it. Step 3
folds it into the probe so this never has to be hand-rolled again:

```python
import importlib.util
spec = importlib.util.spec_from_file_location("pp", "tools/preview_packs.py")
pp = importlib.util.module_from_spec(spec); spec.loader.exec_module(pp)

api = os.environ.get("STORE_API_URL", "https://api.mumchimp.com")
s3, bucket = pp._s3()
ctx = {"api_url": api, "internal_key": os.environ["STORE_INTERNAL_API_KEY"],
       "s3": s3, "bucket": bucket}
for row in pp.fetch_catalogue(api):
    zf, _ = pp.zip_for(row["id"], "r2", ctx)
    qa = zf.read([n for n in zf.namelist() if n.endswith("QA_Report.md")][0]).decode()
    stale = "cleared every check we hold it to" in qa
```

Two traps that cost time here: the env vars are `STORE_INTERNAL_API_KEY` and `STORE_API_URL`
(`tools/preview_packs.py:49,280`) — guess the names and `_content_key` silently falls through to a
prefix listing and returns `None` for all 59, which reads as *"0 stale"*. And `pp.CACHE` persists
zips between runs, so override it to re-prove a fix against the bucket rather than against
yesterday's bytes.

---

# Revision 2 — re-verified from R2, same item, **new sequencing constraint**

**Re-measured:** 2026-08-15, later session · **Status:** still OPEN, both acceptance gates re-run
and both FAIL today · **Verdict:** the item above is unchanged and is still the single highest-leverage
ship item. Three things below are new.

## 1. The census reproduced, twice, cold, from R2 — identical both runs

Independent re-run of the census (script `/tmp/r2_census.py`, built on `tools/preview_packs.py`
`fetch_catalogue` / `_s3` / `zip_for`, `pp.CACHE` overridden to a fresh directory each run so every
zip was genuinely re-fetched):

```json
{ "catalogue_packs": 59, "fetch_errors": 0,
  "zips_with_any_md": 59, "zips_with_QA_Report_md": 59, "zips_with_index_html": 59,
  "clean_sheet_banner": 59, "banner_AND_failed_check": 11,
  "offenders": ["904b6a0bd834ae08","2abc23c3c0d05bab","1723d378cff66ebc","08b22037fc2afc07",
   "13d41ccee9e96e2d","cee21a292219c61a","871ba1e244fba2d4","8d5e24fbe6c1f5d3",
   "d4ef1efc5328d8cf","86a2b4eb9a66df28","176ec4f0fb04c4a5"] }
```

Run 2 (different cache dir) returned the same object, offender list included. 59 of 59 live packs
assert the clean sheet; 11 of them record a failed check in the same surface.

The blocker is also unchanged, reproduced today against the real records:

```
stored PASS dossiers=64 rendered_ok=0 raised=64
FIRST FAILURE: store/dossiers/08b22037fc2afc07.pass.json
  File "prospector/dossier.py", line 671, in render_markdown
    for ax, val in sc.scores.items():
AttributeError: 'types.SimpleNamespace' object has no attribute 'items'
```

## 2. NEW — do the QA re-render **before** the .md-removal backfill, or the false claim is permanent

`5db0a34` ("nine documents, five files, and no markdown in the buyer's zip") is **code only**. The
shelf has not been converted: the census shows all 59 live zips still carry `.md` *and*
`index.html`. So both backfills are still pending, and their order now matters:

- The conversion's render INPUT is the zip's own `.md`, and it does **not** write them back
  (`tools/backfill_bundle_html.py:256-260`).
- It is **one-way** by design: a source zip with no `.md` returns `None`, so a converted pack can
  never have its reader re-rendered by this tool again (`:262-264`, `:293-296`, `:298-299`).

Convert first and the sentence *"This cleared every check we hold it to"* is composed verbatim into
`index.html` on all 59 packs, and the `.md` it came from is gone from the object. Fixing it then
needs a full regenerate, not a backfill. **Ordering: fix the QA text first (or in the same pass),
convert second.**

## 3. NEW — acceptance step 1 as written does not detect the blocker

Re-run today:

| Gate | Command | Result |
|---|---|---|
| Acceptance 1 | `.venv/bin/python -m pytest -q tests/unit/test_pack_manifest.py tests/unit/test_backfill_bundle_html.py` | **42 passed** — green |
| Live records | render every `store/dossiers/*.pass.json` | **raised=64 / 64** — red |
| Acceptance 2 | `.venv/bin/python scripts/pack_banner_probe.py --from r2` | **exit 2**, `unrecognized arguments: --from r2` |
| Disk probe | `.venv/bin/python scripts/pack_banner_probe.py` | **exit 1**, `retired banner: 73 …contradicts it: 12` |

Those unit tests build dossiers from fixtures, whose shape is not the shape `pack_manifest._ns`
produces from a stored record — which is exactly why they stay green while 64 of 64 real records
fail. **Amend acceptance 1** to iterate `store/dossiers/*.pass.json` and assert `raised == 0`. A gate
that cannot fail on the live defect is not the gate.

`scripts/pack_banner_probe.py` is still untracked
(`git ls-files --error-unmatch scripts/pack_banner_probe.py` → *"did not match any file(s) known to
git"*, exit 1), so step 4 of the work order stands as written.

## The work order, re-ordered

1. `_mapping()` reader in `prospector/dossier.py` — acceptance is `raised=0` over the 64 stored
   records, not over fixtures.
2. Re-render `QA_Report.md` from the stored record in the backfill (banner-contradiction case only).
3. Probe `--from {disk,r2}`, default `r2`; commit it; wire it into the `site_spec_probe.py` gate.
4. Re-upload, re-verify from R2 cold — `pack_banner_probe.py --from r2` exits 0.
5. **Only then** run the `.md`-removal conversion over the shelf.

---

# Revision 3 — independently re-verified, later session 2026-08-15

**Status:** still OPEN. Every claim in Revisions 1–2 was re-measured from scratch this session
(fresh census script, `/tmp/r2_census2.py`, not the one Rev 2 used) and every one held. **No edit
was made to the item; this revision adds only receipts.** The ship item is unchanged.

## The census, re-derived and run twice cold — byte-identical to Rev 2

`pp.CACHE` overridden to a *new* `tempfile.mkdtemp()` per run, so both runs re-fetched all 59 zips
from R2. Both runs returned the same object, offender list and order included:

```json
{"banner_AND_failed_check": 11, "catalogue_packs": 59, "clean_sheet_banner": 59,
 "fetch_errors": 0, "zips_with_QA_Report_md": 59, "zips_with_any_md": 59,
 "zips_with_index_html": 59,
 "offenders": ["904b6a0bd834ae08","2abc23c3c0d05bab","1723d378cff66ebc","08b22037fc2afc07",
  "13d41ccee9e96e2d","cee21a292219c61a","871ba1e244fba2d4","8d5e24fbe6c1f5d3",
  "d4ef1efc5328d8cf","86a2b4eb9a66df28","176ec4f0fb04c4a5"]}
```

`zips_with_any_md: 59` and `zips_with_index_html: 59` independently re-confirm Rev 2 §2: `5db0a34`
is code-only, the shelf is unconverted, and the ordering constraint (QA text first, conversion
second) still binds.

**One trap Rev 2 did not record, and it cost a run here.** `pp.CACHE` is a `pathlib.Path` used as
`CACHE / f"{pid}.zip"` (`tools/preview_packs.py:115`). Overriding it with the *string* that
`tempfile.mkdtemp()` returns raises `TypeError: unsupported operand type(s) for /: 'str' and 'str'`
inside `zip_for`, which the census's per-row `except` swallows — the first run reported
`fetch_errors: 59, clean_sheet_banner: 0`, i.e. **it reads exactly like "the defect is gone."**
Wrap the override in `pathlib.Path(...)`, and never accept a census whose `fetch_errors` is
non-zero as evidence of anything.

## The four gates, re-run this session — exit codes captured without a pipe

| Gate | Exact command | Real result |
|---|---|---|
| Blocker | render every `store/dossiers/*.pass.json` via `pack_manifest._ns` + `dossier.render_markdown` | `stored PASS dossiers=64 rendered_ok=0 raised=64` — **red**, same 64 as Rev 2 |
| Acceptance 2 | `.venv/bin/python scripts/pack_banner_probe.py --from r2` | **exit 2**, `unrecognized arguments: --from r2` |
| Disk probe | `.venv/bin/python scripts/pack_banner_probe.py` | **exit 1**, `retired banner: 73 …contradicts it: 12` |
| Probe tracked? | `git ls-files --error-unmatch scripts/pack_banner_probe.py` | **exit 1**, `did not match any file(s) known to git` |

First failure, verbatim, unchanged from Rev 2:

```
FIRST FAILURE: store/dossiers/08b22037fc2afc07.pass.json
  File "prospector/dossier.py", line 671, in render_markdown
    for ax, val in sc.scores.items():
AttributeError: 'types.SimpleNamespace' object has no attribute 'items'
```

## NEW — *why* the disk probe cannot ever be the gate, at a line number

`main()` builds its population from `store/listings/*.json` (`scripts/pack_banner_probe.py:73`) and
its bytes from `_bundle_zip` → `publish/bundles/` (`:56-64`). Both are local build state. That is
the whole of the 77-vs-59 / 73-vs-59 / 12-vs-11 disagreement between the two rows above: **the
probe and the product are measuring different sets of packs.** Work-order step 3 is therefore not
polish — until `--from r2` exists, there is no command that can pronounce this item done.

## Runner-up, re-measured (still not the ship item)

`.venv/bin/python tools/verify_pass_shelf_coverage.py` → **exit 1**, `shelf packs: 59`,
`stranded passes: 5` — identical to Rev 1's list: `25363e54b649587a` (lint blocked, 6 errors:
placeholders, shelf_copy, title_claim) and four never-published with no lint record
(`3d20db251950c20a`, `5b8720247589ae96`, `142717e797740247`, `96a6e230dc6ea48f`). 5 packs against
59; four need a diagnosis before a fix exists. Unchanged verdict: runner-up, not the ship item.

## Verdict

The single highest-leverage next ship item is still **step 1 of the re-ordered work order** — the
`_mapping()` reader in `prospector/dossier.py`, accepted at `raised=0` over the 64 stored records,
because nothing downstream (re-render, re-upload, R2 probe, `.md` conversion) can start until a
stored dossier can be read. It is the only item on this page with no dependency and 59 packs behind
it.
