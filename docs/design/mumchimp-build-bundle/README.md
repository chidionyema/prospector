# Mumchimp build bundle

- `MASTER-BRIEF.md` — the only instruction file. Give this to the coding agent.
- `mockups/` — 12 finished pages. Open any in a browser. `index.html` is the landing page.

Start the agent with the prompt in §11 of MASTER-BRIEF.md.
